<% if logged_in? %>
  <p>ログイン完了ユーザー:<%= current_user.name %></p>
<% else %>
<div class="jumbotron">
  <div class="text-center">
      <h1>Welocome to TaskList</h1></h1>
      <%= link_to "Sign Up", signup_path , class: "btn btn-primary" %>
  </div>
</div>
<% end %>
