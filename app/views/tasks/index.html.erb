<h2>タスクリスト</h2>

<ul>
  <% @tasks.each do |task| %>
  <li><%= link_to task.id, task_path(task)  %>:<%= task.status %>&gt;<%=  task.content %></li>
  <% end %>
</ul>
<%= paginate @tasks %>

<%= link_to "新規追加",new_task_path %>