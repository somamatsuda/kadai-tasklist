<h2>id: <%= @task.id %> のメッセージ編集ページ</h2>

<%= form_with(model: @task, local: true) do |f| %>
  <%= f.label :content, 'タスク' %>
  <%= f.text_field :content %>

  <%= f.submit '変更' %>
<% end %>

<%= link_to '一覧に戻る', "/" %>